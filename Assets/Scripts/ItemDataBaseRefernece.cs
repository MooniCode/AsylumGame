using UnityEngine;

public class ItemDataBaseRefernece : MonoBehaviour
{
    [SerializeField] ItemDatabase itemDatabase;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
    }
}
